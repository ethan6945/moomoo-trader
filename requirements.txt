# DEVELOPMENT deps — intentionally loose, follows upstream.
#
# ⚠ Do NOT use this file for a packaged build or to reproduce a backtest
# result: use requirements.lock (exact `==` pins). The floors below have
# already drifted a major version from what is actually installed and traded
# (pandas 2.2->3.0, numpy 1.26->2.4, moomoo-api 9.3->10.8), and this project's
# validation rests on live<->backtest numerical parity. See requirements.lock.

moomoo-api>=9.3.5308
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=16.0.0          # parquet engine — sandbox kline/VIX cache (data/sandbox_cache)
pandas-ta-classic>=0.3.0
google-genai>=1.0.0      # Gemini SDK — src/ai.py `from google import genai`
optuna>=4.0.0            # nightly optimizer (src/optimizer.py)
python-telegram-bot>=21.0
python-dotenv>=1.0.0
APScheduler>=3.10.0
pytz>=2024.1
requests>=2.31.0
yfinance>=0.2.40
lxml>=5.0.0
matplotlib>=3.8.0
flask>=3.0.0
# (xgboost / scikit-learn / joblib removed 2026-06-03 with the inert ML subsystem)
#
# FinBERT (src/news_score_local.py, FINBERT_ENABLED, default off) is
# deliberately NOT listed here. transformers + torch add ~1-2 GB to the frozen
# bundle, and packaging/mmt-backend.spec excludes matplotlib to save 53 MB —
# shipping a deep-learning stack for an advisory cross-check is not that trade.
# The import is lazy and every failure path degrades to "unavailable", so:
#     pip install transformers torch    # opt in, source installs only
# (rumps / pywebview removed 2026-07-20 — replaced by the native SwiftUI shell in macos/)
